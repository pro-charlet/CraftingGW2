<div class="bloc">
  <div class="titre"><h2>Consulter un item</h2></div>
  <table><tr>
  <%= form_for(Item.new) do |f| %>
    <td><%= text_field_with_auto_complete :item, :name, { :size => 35 }, { :url => items_path(:js), :method => :get, :with => "'search=' + element.value" } %></td>
    <td><%= f.submit "Consulter/Créer", :method => 'POST' %></td>
  <% end %>
  </tr></table>
</div>
